version: 2.1

orbs:
  dwave: dwave/orb-examples@dev:60511e8

workflows:
  version: 2.1
  tests:
    jobs:
      - dwave/test-linux
      - dwave/test-osx
      - dwave/test-win

  weekly:
    triggers:
      - schedule:
          cron: "0 6 * * 0"
          filters:
            branches:
              only:
                - master
                - main
    jobs:
      - dwave/test-linux:
          testing-framework: pytest
          integration-tests: canary
      - dwave/test-osx:
          testing-framework: pytest
          integration-tests: skip
      - dwave/test-win:
          testing-framework: pytest
          integration-tests: skip
